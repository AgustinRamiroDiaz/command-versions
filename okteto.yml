deploy:
  commands:
    - echo "Deploying..."
    - echo ${OKTETO_TOKEN}
    - echo "user: ${OKTETO_USER}"
    - echo ${HOME}
    - echo "kubectl version:"
    - kubectl version
    - echo "helm version"
    - helm version
